#!/usr/bin/python3
"""Class Definition called Square"""


class Square:
    """Private instance attribute: size.
    Instantiation with size (no type/value verification).
    """

    def __init__(self, size):
        """Initializes the __size variable to size value."""
        self.__size = size
